# خطة إصلاح شاملة لمنصة أحلى شباب

## المرحلة 1: دفع الهجرات للإنتاج (السبب الجذري لمعظم العيوب)

الهجرات 0276→0313 محلية ولم تُدفع — تحتوي إصلاحات حرجة. سأستخدم `supabase db push` عبر Management API (يتجاوز مشكلة DNS/IPv6).

**خطوات:**
1. `supabase link --project-ref ujzzvqsodyhnnnpkoaml` (ربط المشروع)
2. `supabase db push` لدفع كل الهجرات المعلقة
3. `notify pgrst, 'reload schema'` لتحديث PostgREST
4. التحقق: `select public.kpi_diag_run('2026-08-01'::date)`

**النتيجة المتوقعة:** إصلاح صفحة تفاصيل الموظف (get_employee_360)، إصلاح KPI grants، phone normalization، avatar RPC، org chart.

## المرحلة 2: إصلاح عيوب الواجهة (admin_web)

### 2a. شارات حالة KPI بالعربية — `KpiCyclesPage.tsx`
- السطر ~233: `StatusBadge value={cycle.status}` يعرض "open"/"locked" بالإنجليزية
- **الإصلاح**: إنشاء خريطة `cycleStatusLabels` تترجم: open→مفتوح، locked→مغلق، draft→مسودة، closed→مغلق، archived→مؤرشف
- تطبيقها في `StatusBadge value={cycleStatusLabels[cycle.status] ?? cycle.status}`

### 2b. تكرار "آخر سبب إداري:" — `KpiCyclesPage.tsx`
- السطر 242: `<p>آخر سبب إداري: {cycle.overrideReason}</p>` لكن `overrideReason` نفسه يبدأ بـ "آخر سبب إداري:"
- **الإصلاح**: إزالة البادئة الثابتة، الاعتماد على القيمة من DB فقط: `<p>{cycle.overrideReason}</p>` أو تنظيفها بـ `replace(/^آخر سبب إداري:\s*/, '')`

### 2c. تعريب "Onboarding" في القائمة الجانبية
- البحث في ملف التوجيه/القائمة عن "Onboarding" وتعيين "التهيئة"
- التحقق من `WorkspaceShell.tsx` أو ملف التوجيه الرئيسي

### 2d. عمود الكود في جدول الموظفين — `EmployeesPage.tsx`
- العمود يعرض `phone_e164` بدل `employee_code`
- **الإصلاح**: تغيير المصدر من `row.phoneE164` إلى `row.employeeCode` في عمود "الكود"

### 2e. تحسين errorMapper.ts
- إضافة نمط `/permission denied for function/i` → "ليس لديك صلاحية لهذا الإجراء."
- التأكد من أن `NO_DATA_FOUND` و `P0002` يتم التقاطهما

## المرحلة 3: فحوصات إضافية بالكود

- التحقق من عدم وجود `dangerouslySetInnerHTML` (لم يُرصد — جيد)
- التحقق من عدم وجود مفاتيح سرية hardcoded (لم يُرصد — جيد)
- فحص `flutter analyze` للتطبيق (إن أمكن)
- فحص RLS على الجداول الجديدة في الهجرات 0276-0313

## المرحلة 4: البناء والنشر

1. `npm run build` للتأكد من عدم وجود أخطاء TypeScript
2. `vercel deploy --prod` لنشر الإصلاحات
3. التحقق من الرؤوس والموقع المباشر

## المرحلة 5: التحقق النهائي

- فتح صفحة تفاصيل موظف في المتصفح → يجب أن تعمل
- فتح صفحة KPI cycles → يجب أن تظهر الشارات بالعربية بلا تكرار
- فتح جدول الموظفين → يجب أن يعرض الكود الصحيح
- فحص console لأخطاء CSP → يجب ألا تكون موجودة

## ملاحظات
- **لن أعد ترقيم أي هجرة** (zero-renumber policy)
- **لن أعدل أي هجرة مدفوعة** — كل الإصلاحات الجديدة ستكون في ملفات جديدة أو في كود الواجهة
- **الأخطاء الإملائية في بيانات الموظفين** (مثل "أدارة" بدل "إدارة") هي بيانات من إدخال المستخدم، لا كود — سأذكرها فقط
- **تدوير المفاتيح** مسؤولية المستخدم من لوحات Supabase/Vercel