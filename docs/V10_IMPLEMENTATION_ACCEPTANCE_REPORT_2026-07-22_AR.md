# تقرير تنفيذ وقبول الخطة V10

التاريخ: 2026-07-22  
الفرع: `fix/p0-location-security-pipeline`  
مرجع الخطة: `AHLA_SHABAB_CURRENT_ENVIRONMENT_MASTER_PLAN_V10 (1).md`

## النتيجة التنفيذية

تم تنفيذ الخطة ونشرها داخل المشروع الحالي من دون إنشاء تطبيق أو قاعدة بيانات جديدة، ومن دون حذف جداول أو تغيير `Package Name` أو التوقيع. نُشرت مهاجرات قاعدة البيانات حتى `0115` على مشروع Supabase الحالي، ونُشرت لوحة Production على مشروع Vercel الحالي، وبُنيت حزم Android بالتوقيع الأصلي وتحققت بصمة شهادتها.

## الحماية وخط الأساس

- النسخة الاحتياطية للمشروع: `D:\Coder\HR\_backups\HR_Platform_2_pre_v10_20260722_0605.tar.gz`.
- نسخة Schema المحلية: `D:\Coder\HR\_backups\supabase_local_schema_20260722_0605.sql`.
- نسخة البيانات المحلية: `D:\Coder\HR\_backups\supabase_local_data_20260722_0605.sql`.
- نسخة Supabase البعيدة قبل النشر (Schema/Data/Roles): `D:\Coder\HR\_backups\HR_Platform_2_pre_release_20260722_143101`.
- نسخة Supabase البعيدة قبل تحديث الصور (Schema/Data/Roles): `D:\Coder\HR\_backups\HR_Platform_2_pre_avatar_20260723_055823`.
- نقطة استعادة Supabase البعيدة بعد `0115` (Schema/Data/Roles): `D:\Coder\HR\_backups\HR_Platform_2_post_0115_20260723_090439`.
- Baseline Git: `8c30e8f`.
- إصلاحات P0 والأمن والحضور: `3b692f1`.
- تنفيذ KPI والتقرير التنفيذي: `edd6a89`.
- الإجازات والمأموريات واختبار الأدوار الستة: `f804bf7`.
- إصلاح تهيئة مصفوفة أخطاء KPI واختباره: `6eae5b7`.

## مطابقة البنية

| بند V10 | الحالة | الدليل |
|---|---|---|
| لوحة ويب واحدة لـHR والسكرتير بصلاحيات مختلفة | مكتمل | Workspace guards + RLS + اختبار HR/Main Admin |
| تطبيق Flutter واحد للموظف والمدير وOperations والمدير التنفيذي | مكتمل | `get_my_access_context` ومساحات العمل الموروثة |
| المدير التنفيذي بلا حضور أو بصمة | مكتمل | سياسة الخادم + اختبار عدم إنشاء سجل حضور |
| Operations يرث الموظف والمدير وله التصعيد واللجنة | مكتمل | مساحات Employee/Manager/Field Operations/Committee واختبار Runtime |
| لا قاعدة أو تطبيق أو Package Name جديد | مكتمل | نفس Supabase ونفس Flutter؛ `org.ahlashabab.ahla_shabab_management_os` |

## KPI V10

- المسار الفعلي: `self → manager_review → hr_review → manager_final → finalized → closed → archived`.
- الموظف يقيّم البنود السبعة ذاتيًا.
- المدير يحرر بنوده الأربعة ويرسل إلى HR ثم يعتمد نهائيًا.
- HR يحرر الصلاة والحلقة والحضور الآلي فقط.
- المدير لا يعتمد تقييمه الشخصي.
- المدير التنفيذي لا يعتمد الدرجات ويقرأ التقرير المجمع فقط.
- فتح الدورة وإغلاقها وتمديدها وتعليقها وإعادة فتحها وأرشفتها حصري للسكرتير التنفيذي، وكل تدخل مسجل في Audit.
- التقرير التنفيذي اليومي يبدأ من جميع الموظفين النشطين ويعرض الحضور والإجازات والمأموريات والقوافل والفاندي والطلبات وKPI والقضايا والتقارير وطلبات الموقع مع المصدر ووقت التحديث.

المهاجرات: `0109` و`0110` و`0111` و`0115`.

## الإجازات والمأموريات

- تقديم الإجازة والمأمورية بالتاريخ والسبب والمرفقات الخاصة.
- اختيار البديل، وعرض البديل والتعارضات الفعلية للمدير.
- سحب الطلب قبل القرار.
- سبب رفض إلزامي في قاعدة البيانات، وليس في الواجهة فقط.
- منع اعتماد الطلب الشخصي.
- إنشاء مأمورية/قافلة/فاندي للفريق من وحدة تكليفات العمل.
- التصعيد بعد المهلة إلى Operations، وتسجيل اسم موظف Operations الحقيقي وعدم نسبة القرار إلى المدير التنفيذي.
- المرفقات في Bucket خاص مع RLS وSigned URL قصير العمر.

المهاجرات: `0112` و`0113`.

## لجنة حل المشكلات

- الموظف والمدير وOperations يستطيعون تقديم مشكلة، واختيار الأطراف والشهود وإضافة صور/أدلة.
- الإلغاء متاح قبل قبول اللجنة فقط.
- عضو Operations يرى القضية المسندة ويعمل داخل Workflow اللجنة.
- الجلسات والمحاضر والتوصيات والقرارات والتنفيذ والاستئناف موجودة ومحمية بالصلاحيات.
- المدير التنفيذي يعمل من مركز القرار، وليس من نموذج تقديم المشكلة.

## دليل قبول الأدوار الستة

الاختبار `supabase/tests/0045_v10_six_role_acceptance.sql` أنشأ ستة حسابات فعلية داخل معاملة اختبارية وشغّل RLS وRPCs لكل دور، ثم تراجع عن البيانات. نجحت 47 حالة تشمل:

| الدور | دليل التنفيذ |
|---|---|
| الموظف | إجازة، مأمورية، مشكلة، مرفق، بديل، سحب قبل القرار، منع الاعتماد الذاتي |
| المدير | فريقه فقط، المرفق والبديل والتعارض، قبول الإجازة، رفض المأمورية بسبب إلزامي |
| Operations | توريث الموظف والمدير، القضية المسندة، قرار مصعد بالإنابة باسمه الحقيقي |
| المدير التنفيذي | لا حضور، تقرير V10، طلب موقع لأي موظف |
| HR | Web فقط، مرحلة HR، منع فتح دورة KPI، صلاحيات الموظفين المسموحة |
| السكرتير التنفيذي | Main Admin فقط، فتح وتمديد دورة KPI حصريًا |

## نتائج التحقق

- TypeScript typecheck: ناجح.
- اختبارات العقود والويب الحالية: 59/59 ناجحة.
- Web production build: ناجح.
- Flutter analyze: لا أخطاء ولا تحذيرات.
- Flutter tests الحالية: 29/29 ناجحة.
- Supabase DB tests المحلية والبعيدة: 47 ملفًا، 636 اختبارًا ناجحًا داخل معاملات تنتهي بـ`ROLLBACK`.
- Remote DB lint على `public`: صفر أخطاء.
- المهاجرات المحلية والبعيدة متطابقة حتى `0115`، ولم توجد مهاجرات معلقة في فحص ما بعد النشر.

## حالة Release والنشر

- `applicationId` و`namespace`: `org.ahlashabab.ahla_shabab_management_os` بلا تغيير.
- الإصدار: `0.11.1+12`.
- الـKeystore الحالي محفوظ في `apps/mobile_flutter/android/app/release-keystore.jks` ولم يُستبدل.
- Supabase: تم تطبيق `0104–0115` على المشروع الحالي، وتوجد نسخ Schema/Data/Roles قبل النشر وبعد `0115`، ونجحت المحاكاة اللاحقة بلا تغييرات معلقة.
- Vercel Production: `https://ahla-shabab-management-os.vercel.app/admin`، واستجاب المسار والأصل البرمجي بالحالة `200` مع رؤوس الأمان المطلوبة.
- Edge Functions: نُشرت `passkey-register` و`verify-attendance-punch` و`webauthn-challenge`، واختبار الطلب غير الموثق أعاد `401` لكل دالة.
- Android APKs: `app-armeabi-v7a-release.apk` و`app-arm64-v8a-release.apk` و`app-x86_64-release.apk`.
- Android App Bundle: `apps/mobile_flutter/build/app/outputs/bundle/release/app-release.aab`.
- جميع APKs تستخدم توقيع V2/V3، والحزمة والإصدار والمعمارية صحيحة.
- بصمة SHA-256 لشهادة التوقيع: `99:E8:C4:4A:17:C8:AF:1E:62:82:3F:94:93:25:07:C7:E2:02:0E:D0:93:1A:B0:4A:BD:BC:A4:3E:86:3B:89:88`.
- بصمة SHA-256 لملف AAB النهائي: `3F645A4782DA9D1220633D51F0D985EAE5CA597DBB7B443955F1868E1E0B3637`.

## تحديث الواجهة والصور — 2026-07-23

- توحيد الصور الشخصية بين الويب وFlutter على `employee-avatars` مع قص مربع، حد 5 MiB، والتحقق من الدقة والصيغة.
- الموظف يدير الصور داخل مجلد `auth.uid()` الخاص به فقط، بينما HR/Admin يحتفظان بصلاحيات الإدارة؛ يغطي الاختبار `0046` حدود RLS كاملة.
- إضافة صورة المستخدم الفعلية مع fallback آمن في القوائم والملف الشخصي ولوحة الإدارة.
- تثبيت الوضع الفاتح/الداكن قبل رسم الواجهة لمنع الوميض، وتحسين تباين الوضع الداكن واختباره آليًا.
- توحيد أهداف اللمس المهمة عند 44px وتحسينات الاستجابة للهواتف.
- حفظ تفضيل الثيم في Flutter وإجراء فحص اتصال فوري عند بدء التطبيق مع دعم Android/Web.

## حدود التحقق الآلي

اكتمل النشر والبناء والتحقق الآلي. يبقى قبول بشري على جهاز Android فعلي لمسارات الكاميرا وGPS وPasskey وPush Notifications قبل توزيع الحزمة على المستخدمين، ثم رفع AAB إلى حساب المتجر عند اعتماد جهة التشغيل؛ لا يتوفر جهاز Android أو حساب متجر ضمن بيئة التنفيذ الحالية.
