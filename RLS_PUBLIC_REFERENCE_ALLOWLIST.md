# RLS Public Reference Allowlist — قائمة الجداول المرجعية العامة

> V23 وكيل 01 — توثيق كل جدول يستخدم `USING(true)` مع المبرر.
>
> **المبدأ:** Default Deny. لا يُسمح بـ`USING(true)` إلا على جداول القراءة المرجعية
> التي يحتاجها كل مستخدم مصادَق لعمل الواجهة الأمامية، ولا تحتوي بيانات شخصية.
> أي جدول جديد يحتاج `USING(true)` يجب أن يُوثَّق هنا أولاً مع المبرر.

## الجداول المعتمدة

### 1. نظام الأدوار والصلاحيات (0002)

| الجدول | السياسة | المبرر |
|--------|---------|--------|
| `permissions` | `permissions_read` | كتالوج الصلاحيات — لا بيانات شخصية. يحتاجها محرر الأدوار لعرض الصلاحيات المتاحة. الكتابة ممنوعة (seed/service_role فقط). |
| `roles` | `roles_read` | كتالوج الأدوار — لا بيانات شخصية. يحتاجها كل الواجهات لعرض أسماء الأدوار. الكتابة عبر `rpc_upsert_role` فقط. |
| `role_permissions` | `role_perms_read` | ربط الأدوار بالصلاحيات — لا بيانات شخصية. ضروري لعرض صلاحيات كل دور. الكتابة عبر `rpc_set_role_permissions` فقط. |

### 2. الهيكل التنظيمي (0003)

| الجدول | السياسة | المبرر |
|--------|---------|--------|
| `legal_entities` | (loop-generated) | كيانات قانونية — بيانات مرجعية تنظيمية عامة. |
| `branches` | (loop-generated) | فروع — بيانات مرجعية تنظيمية عامة. |
| `departments` | (loop-generated) | إدارات — بيانات مرجعية تنظيمية. تظهر في الدليل والهيكل التنظيمي. |
| `teams` | (loop-generated) | فرق — بيانات مرجعية تنظيمية. |
| `positions` | (loop-generated) | مناصب — بيانات مرجعية تنظيمية. |
| `job_titles` | (loop-generated) | مسميات وظيفية — كتالوج مرجعي. |
| `grades` | (loop-generated) | درجات وظيفية — كتالوج مرجعي. |

> **ملاحظة:** `geofences` استُثني من الحلقة في 0003 — له سياسة خاصة تقيّد القراءة بالمدير المباشر + full-access.

### 3. سير العمل والإجازات (0006)

| الجدول | السياسة | المبرر |
|--------|---------|--------|
| `leave_types` | `leave_types_select` | أنواع الإجازات — كتالوج مرجعي يحتاجه كل موظف عند تقديم طلب إجازة. |
| `workflow_definitions` | `workflow_definitions_select` | تعريفات سير العمل — كتالوج مرجعي للنظام. |
| `workflow_steps` | `workflow_steps_select` | خطوات سير العمل — كتالوج مرجعي للنظام. |

### 4. الأداء والتقييم (0007)

| الجدول | السياسة | المبرر |
|--------|---------|--------|
| `kpi_templates` | `kpi_templates_select` | قوالب التقييم — كتالوج مرجعي للنظام. |
| `kpi_criteria` | `kpi_criteria_select` | معايير التقييم — كتالوج مرجعي للنظام. |
| `competencies` | `competencies_select` | كتالوج الكفاءات — مرجعي. |
| `competency_levels` | `competency_levels_select` | مستويات الكفاءات — مرجعي. |
| `role_competency_profiles` | `role_competency_profiles_select` | ملفات كفاءات الأدوار — مرجعي. |
| `review_cycle_templates` | `review_cycle_templates_select` | قوالب دورات المراجعة — مرجعي. |

### 5. المستندات والمهام (0009)

| الجدول | السياسة | المبرر |
|--------|---------|--------|
| `task_templates` | `task_templates_select` | قوالب المهام — كتالوج مرجعي. |
| `asset_inventory` | `asset_inventory_select` | جرد الأصول — مرجعي. لا يحتوي بيانات شخصية. |

### 6. النظام والأمان (0011)

| الجدول | السياسة | المبرر |
|--------|---------|--------|
| `feature_flags` | `feature_flags_select` | أعلام الميزات — يحتاجها كل عميل لتحديد الميزات النشطة. لا بيانات حساسة. |

### 7. التعلم (0033)

| الجدول | السياسة | المبرر |
|--------|---------|--------|
| `learning_course_sessions` | `learning_sessions_read` | جلسات الدورات — مرجعي. ملاحظة: `learning_courses` تستخدم فلتر `active` وليس `true`. |

### 8. سياسات KPI (0058)

| الجدول | السياسة | المبرر |
|--------|---------|--------|
| `kpi_policy_versions` | `kpi_policy_versions_read` | نسخ سياسات التقييم — كتالوج مرجعي للنظام. |

### 9. الإجازات الرسمية (0132)

| الجدول | السياسة | المبرر |
|--------|---------|--------|
| `public_holidays` | `public_holidays_select` | الإجازات الرسمية — كتالوج عام يحتاجه كل موظف. |

### 10. الموظف متعدد الإدارات (0156)

| الجدول | السياسة | المبرر |
|--------|---------|--------|
| `employee_departments` | `employee_departments_select` | انتماء الموظفين للإدارات — مرجعي للهيكل التنظيمي. |

---

## الجداول المرفوضة / غير المعتمدة

| الجدول | السبب |
|--------|-------|
| `employees` | يحتوي بيانات شخصية — يستخدم `can_access_employee()` scope-aware. |
| `profiles` | مرتبط بالموظفين — RLS بالمستخدم. |
| `user_roles` | إسنادات الأدوار — RLS: المستخدم يرى أدواره + حامل `access.role.assign` يرى الكل. |
| `audit_events` | سجلات التدقيق — لا يُقرأ إلا من المخوّلين. |
| `dispute_cases` | قضايا شخصية — RLS عبر `can_access_dispute()`. |
| `kpi_evaluations` | تقييمات شخصية — RLS عبر `can_access_employee()`. |
| `attendance_daily` | بيانات حضور شخصية — RLS عبر المستخدم + المدير. |
| `requests` | طلبات شخصية — RLS عبر المستخدم + المدير + النطاق. |
| `geofences` | بيانات مواقع — RLS خاص (مدير + full-access). |
| `live_location_requests` | طلبات موقع حي — RLS خاص (تنفيذي + المستهدف). |

---

## إرشادات الإضافة

1. **أضف الجدول هنا أولاً** قبل كتابة `USING(true)` في أي migration.
2. **المعايير:** لا بيانات شخصية (PII) + كتالوج/مرجع ثابت + يحتاجه كل مستخدم مصادَق.
3. **راجع:** هل يمكن تقييد القراءة بنطاق أصغر دون كسر الواجهة؟ إذا نعم، لا تستخدم `USING(true)`.
4. **الكتابة:** `USING(true)` على SELECT فقط. الكتابة عبر RPCs أو service_role.

---

*آخر تحديث: V23 — وكيل 01 (الأدوار والصلاحيات وRLS)*
