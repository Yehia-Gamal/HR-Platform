# خط أساس الأداء — PERFORMANCE_BASELINE

> **تاريخ الجرد:** 2026-07-27 | **Agent 00A** | **محدّث**

---

## 1. نتائج الاختبارات

### اختبارات العقود المشتركة (vitest)

| الحالة | العدد |
|---|---|
| ✅ ناجح | **90** |
| ❌ فاشل | 0 |
| **المجموع** | **90** |
| الملفات | 17 |

> ⚠️ ملاحظة: في جلسة سابقة كان هناك 3 اختبارات KPI فاشلة (`kpi.test.ts` — ترتيب المراحل + توزيع المقيّمين). تم إصلاحها بين الجلسات — جميع الاختبارات تمر الآن.

### اختبارات الويب (vitest)

| الحالة | العدد |
|---|---|
| ✅ ناجح | **76** |
| ❌ فاشل | 0 |
| **المجموع** | **76** |
| الملفات | 14 |

### المجموع عبر `npm run test`

```
✅ 166/166 اختبار ناجح (90 عقود + 76 ويب) — 0 فاشل
```

### فحص الأنماط (TypeScript)

```
npx tsc --noEmit -p apps/admin_web/tsconfig.json
✅ نجاح — 0 أخطاء
```

### البناء (Build)

```
npm run build  →  tsc -b --force && vite build
✅ نجاح — 1996 وحدة مبنية في 1.40 ثانية
```

| الملف | الحجم | gzip |
|---|---|---|
| `index.js` | 490 KB | 111 KB |
| `react-vendor.js` | 439 KB | 137 KB |
| `supabase.js` | 204 KB | 52 KB |
| `vendor.js` | 79 KB | 25 KB |
| `query.js` | 43 KB | 13 KB |
| `index.css` | 76 KB | 14 KB |

### اختبارات pgTAP

> تتطلب Docker — لم تُشغل في هذه الجلسة.
> آخر حالة معروفة: 65 ملف اختبار / ~870 assertion

### اختبارات Flutter

> تتطلب Flutter SDK — لم تُشغل في هذه الجلسة.
> آخر حالة معروفة: 6 ملفات / 33 اختبار

---

## 2. إحصائيات الاختبارات

| الفئة | الملفات | الاختبارات/Assertions | الحالة |
|---|---|---|---|
| ويب (vitest) | 14 | 76 | ✅ 76/76 |
| عقود مشتركة (vitest) | 17 | 90 | ✅ 90/90 |
| Flutter | 6 | 33 | ⏸ لم تُشغل |
| pgTAP | 65 | ~870 | ⏸ لم تُشغل |
| Edge Functions (phone.test.ts) | 1 | غير معروف | ⏸ لم تُشغل |
| E2E (Playwright) | 2 | غير معروف | ⏸ لم تُشغل |
| **المجموع** | **~105** | **~1,069+** | **166/166 ✅** |

---

## 3. تعقيد الـ Migrations

| المقياس | القيمة |
|---|---|
| إجمالي ملفات Migration | 163 |
| Schema migrations (جداول جديدة) | ~40 |
| RPC migrations (دوال جديدة) | ~30 |
| Fix/Patch migrations | ~80 |
| Seed/Config migrations | ~8 |
| Bridge placeholders | 2 |
| ⚠️ أرقام مكررة | 3 حالات (0060, 0062, 0160) |

### نسبة الإصلاحات

```
Fix/Patch: ~80/163 = 49% من الـ Migrations هي إصلاحات
```

هذه نسبة عالية تشير إلى تطور سريع مع إصلاحات متكررة. الدوال الأكثر إعادة إنشاء:
- `record_attendance_event()` — 5+ مرات
- `request_live_location()` — 3+ مرات
- Employee RLS policies — 3 مرات

---

## 4. حجم الكود

| المكون | الملفات | التقدير |
|---|---|---|
| ويب (src/) | ~60 ملف .tsx/.ts | متوسط |
| Flutter (lib/) | ~80 ملف .dart | متوسط-كبير |
| Migrations (SQL) | 162 ملف | كبير |
| Edge Functions | 12 × index.ts + 3 shared | متوسط |
| اختبارات pgTAP | 65 ملف | كبير |
| عقود مشتركة | 18 ملف مصدر + 17 اختبار | متوسط |

---

## 5. مخاوف أداء معروفة

| المخاوف | التفاصيل | الأولوية |
|---|---|---|
| دوال SECURITY DEFINER كثيرة | 500+ دالة — كل استعلام يمر عبر auth context | مراقبة |
| RLS policy chains | بعض الجداول لها 4 سياسات (CRUD) كل منها تستدعي دوال | مراقبة |
| Geofence calculations | حساب المسافة لكل نقطة حضور | يحتاج فهرس مكاني |
| KPI cycle processing | دورات KPI تشمل جميع الموظفين — دالة batch | مراقبة تحت الحمل |
| FCM notification dispatch | cron worker يعالج قائمة انتظار — retry مع backoff | مراقبة |

---

## 6. التوصيات

1. ~~**إصلاح اختبارات KPI الفاشلة**~~ — ✅ تم الإصلاح (جميع 90 اختبار تمر)
2. **تشغيل pgTAP عند توفر Docker** — التحقق من 870 assertion
3. ~~**قياس أداء البناء**~~ — ✅ تم (1996 وحدة في 1.40 ثانية)
4. **مراقبة حجم الـ Migrations** — 163 ملف يعني وقت `db reset` طويل
5. **إضافة اختبارات E2E** — الملفان الحاليان (app-structure, auth-login) يحتاجان توسيع
6. **تقليل حجم bundle** — `index.js` = 490 KB + `react-vendor.js` = 439 KB ← دراسة lazy loading
