-- Migration 0119: جسر ترقيم (no-op) — لسد فجوة تسلسل الترحيلات.
-- ============================================================================
-- خلفية: أثناء التطوير حُذف/أُعيد ترقيم ترحيل رقم 0119، ما ترك فجوة في التسلسل
-- يرفضها فاحص البنية (scripts/validate-foundation.mjs) الذي يشترط تسلسلاً متصلاً.
-- الترحيلات 0120→0126 دُفعت بالفعل إلى Staging، فلا يجوز إعادة ترقيمها.
-- الحل الآمن والمتوافق خلفياً: ترحيل فارغ بلا أثر جانبي يحفظ اتصال التسلسل.
-- idempotent بالكامل ولا يعدّل أي كائن قائم.
-- ============================================================================

do $$
begin
  -- لا عملية. موجود فقط لسلامة تسلسل الترحيلات.
  null;
end $$;
