-- Migration 0232: جسر ترقيم (no-op) — لسد فجوة تسلسل الترحيلات.
-- ============================================================================
-- خلفية: أثناء عمليات reset خارجية للـ git، فُقد ملف 0232 قبل دفعه.
--        0233/0234 موجودة، فلا يجوز إعادة ترقيمها.
-- الحل الآمن والمتوافق خلفياً: ترحيل فارغ بلا أثر جانبي يحفظ اتصال التسلسل.
-- idempotent بالكامل ولا يعدّل أي كائن قائم.
-- ============================================================================

do $$
begin
  null;
end $$;
