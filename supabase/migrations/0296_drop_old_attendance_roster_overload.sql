-- =============================================================================
-- 0296: إزالة overload القديم get_attendance_day_roster(date,text).
-- =============================================================================
-- منذ 0294 يوجد overload موسّع (date, text, text, uuid, uuid, uuid, text, text,
-- integer, integer) بكل المعاملات اختيارية. بقاء الصيغة القديمة (date,text)
-- يجعل النداء بمعاملين «غير فريد» (42725) على مستوى Postgres/PostgREST.
-- نُزيل الصيغة القديمة ليبقى دالة واحدة فقط؛ كل النداءات القديمة بمعاملين
-- ستستمر عبر الدالة الموسّعة (الباقي افتراضيات).
-- =============================================================================

drop function if exists public.get_attendance_day_roster(date, text);
