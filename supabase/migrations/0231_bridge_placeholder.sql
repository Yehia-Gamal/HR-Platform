-- Migration 0231: جسر ترقيم (no-op) — لسد فجوة تسلسل الترحيلات.
-- ============================================================================
-- خلفية: أثناء عمليات reset خارجية للـ git، فُقد ملف 0231 قبل دفعه.
--        0232/0233/0234 موجودة، فلا يجوز إعادة ترقيمها.
-- الحل الآمن والمتوافق خلفياً: ترحيل فارغ بلا أثر جانبي يحفظ اتصال التسلسل.
-- idempotent بالكامل ولا يعدّل أي كائن قائم.
-- ============================================================================

do $$
begin
  -- لا عملية. موجود فقط لسلامة تسلسل الترحيلات.
  null;
end $$;
