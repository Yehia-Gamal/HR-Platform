-- Migration 0122: جسر ترقيم (no-op) — لسد فجوة تسلسل الترحيلات.
-- ============================================================================
-- خلفية: نفس سبب 0119 — فجوة ترقيم بين 0121 و0123. الحل: ترحيل فارغ no-op.
-- idempotent بالكامل ولا يعدّل أي كائن قائم.
-- ============================================================================

do $$
begin
  null;
end $$;
