-- =============================================================================
-- 0323: (مكرر) — إصلاح لوحة الحضور: isWeekend + سحب منح anon.
-- -----------------------------------------------------------------------------
-- ملاحظة: هذا الملف كُتب موازياً لـ 0320 ويفعل نفس العمل بالضبط (إعادة تعريف
-- get_attendance_dashboard بإضافة isWeekend + revoke تنفيذ get_attendance_day_roster
-- عن anon/PUBLIC). 0320 سبقه رقمياً وهو المطبَّق على القاعدة، لذا 0323 محتفَظ به
-- كتوثيق فقط دون أي تنفيذ — حتى لا يُكرَّر العمل نفسه في السلسلة النظيفة.
-- السلسلة الصحيحة: 0320 (الإصلاح الفعلي) → 0321 → 0322 → 0324.
-- =============================================================================

begin;

-- (لا عمليات — انظر 0320 للإصلاح الفعلي)

commit;
