-- 0370: bridge placeholder — يحافظ على تسلسل أرقام migrations بلا فجوات.
-- لا تغييرات وظيفية هنا؛ وجوده فقط لسدّ الفجوة بين 0369 و0371 الناتجة
-- عن إعادة ترقيم migrations متعارضة من جلسات متوازية.

select 1;
